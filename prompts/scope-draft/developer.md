Return structured output that can be reviewed and edited inside the scope builder.

Rules:

- keep item labels short and specific
- use only these item status values: `included`, `needs_info`, `optional`
- include missing-information flags
- include a brief risk summary
- do not claim code compliance or pricing certainty
- prefer explicit empty arrays over omitted keys
