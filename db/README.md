# Database

Use this folder for schema, migrations, seeds, and data model notes.

If Prisma is adopted, place the schema or migration entrypoint here rather than scattering it across feature modules.
